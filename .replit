run = "bash -c 'echo \"🔍 Checking Python...\"; which python3 || which python || find /usr /opt /nix/store /home/runner -name python3 -type f -executable 2>/dev/null | head -1 || echo \"❌ Python not found\"; echo \"🚀 Starting app...\"; PYTHON_CMD=\$(which python3 || which python || find /usr /opt /nix/store /home/runner -name python3 -type f -executable 2>/dev/null | head -1); \$PYTHON_CMD -m uvicorn app.main:app --host 0.0.0.0 --port \$PORT'"

[build]
command = "pip install -r requirements.txt && apt-get update && apt-get install -y wkhtmltopdf"

[env]
PYTHONUNBUFFERED = "1"
PORT = "8000"
