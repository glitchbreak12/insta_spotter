run = "./start.sh"

[nix]
modules = ["python-3.11"]

[build]
command = "pip install -r requirements.txt && apt-get update && apt-get install -y wkhtmltopdf"

[env]
PYTHONUNBUFFERED = "1"
PORT = "8000"
